package com.acrescrypto.zksync.net;

public class TCPPeerSocketListenerTest {
	// begins listening when constructor called
	// listens on requested port when specified

	// uses random port when 0 requested
	// stops listening when close called
	// getPort returns current port
	
}
